# Worker Node Configuration

# IP address of the Swarm manager node
MANAGER_IP=1.2.3.4

# Worker join token — get this from the manager by running:
#   docker swarm join-token worker
SWARM_JOIN_TOKEN=SWMTKN-1-xxxxxx-xxxxxx
